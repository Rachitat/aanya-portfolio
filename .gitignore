# Note: supabase-config.js is committed on purpose — GitHub Pages needs it in the
# repo to connect to Supabase. The anon key is safe to publish; protect data with
# Row Level Security in Supabase (never commit the service_role key).

# macOS clutter
.DS_Store

# Windows clutter
Thumbs.db

# if we ever install npm packages
node_modules/

# generic secrets file
.env

# local-only secrets
.env.local
